server:
  port: 9195
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SanskarxRawat/micro-configuration
          clone-on-start: true
